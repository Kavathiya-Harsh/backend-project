services:
  - type: web
    name: notes-app
    env: node
    plan: free
    rootDir: notes-app
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
